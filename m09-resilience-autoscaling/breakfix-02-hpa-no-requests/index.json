{
  "title": "M09 — Break/fix 02: HPA Can't Read Its Metric",
  "description": "transcode-scaler's HorizontalPodAutoscaler is stuck at <unknown>/50% and never scales. The target has no CPU request, so there's no denominator for a utilization percentage. Add the request.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the dead autoscaler", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
