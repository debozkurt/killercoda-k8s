{
  "title": "M16 — Break/fix 02: Generator Name Mismatch",
  "description": "edge-relay rendered and applied fine, but its Pod is stuck in CreateContainerConfigError. The generated ConfigMap exists under a hashed name; the Deployment asks for a name that doesn't. Find why Kustomize didn't rewrite the reference.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Trace the missing ConfigMap", "text": "step1/text.md" },
      { "title": "Align the names and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
