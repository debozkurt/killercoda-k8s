{
  "title": "M05 — Break/fix 02: Pod Names a Claim That Isn't There",
  "description": "directory is stuck Pending because it names a claim that doesn't exist. Read describe pod and get pvc to see the Pod points at a claimName that was never created, and repoint it at the claim that's actually there.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the missing claim", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
