{
  "title": "M05 — Break/fix 03: RWO Volume Can't Span Two Nodes",
  "description": "directory was scaled to 2 replicas sharing one ReadWriteOnce PVC. The claim is Bound, yet one replica is stuck — an RWO volume can't be attached on a second node. Read the Bound-but-stuck signature and fix the access-mode mismatch.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the Bound-but-stuck volume", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
