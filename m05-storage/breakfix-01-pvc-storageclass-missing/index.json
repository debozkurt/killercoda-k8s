{
  "title": "M05 — Break/fix 01: PVC Won't Bind (Missing StorageClass)",
  "description": "cdr-writer is stuck Pending and never starts. Its PVC is Pending too — it names a StorageClass that doesn't exist, so no volume is ever provisioned. Find it with get pvc / describe pvc and fix the class.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the unbound PVC", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
