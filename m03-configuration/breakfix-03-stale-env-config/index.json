{
  "title": "M03 — Break/fix 03: config edited, nothing changed",
  "description": "A ConfigMap was updated, but the workload still serves the old value. Diagnose the propagation gap — env is frozen at container start — and make the change take.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the stale value", "text": "step1/text.md" },
      { "title": "Make the change take, and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
