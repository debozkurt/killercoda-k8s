{
  "title": "M13 — Break/fix 02: Logs & Events — a crashlooping sidecar",
  "description": "sip-monitor is stuck at 1/2. The nginx app container is healthy; its metrics-agent telemetry sidecar crashloops. Read which container the events name, then logs -c metrics-agent --previous to see why the dead instance exited, and fix the sidecar's command.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Find the failing container", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
