{
  "title": "M18 — Break/fix 03: HelmRelease Dependency",
  "description": "A HelmRelease is stuck not-ready and never installs, though the source, the apps Kustomization, and its message-store dependency are all healthy. Read the dependsOn message and correct the reference.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Why the release won't install", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
