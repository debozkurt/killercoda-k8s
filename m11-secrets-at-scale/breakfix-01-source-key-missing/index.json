{
  "title": "M11 — Break/fix 01: SecretSync SyncError (Missing Source Key)",
  "description": "The partner-connector Pod (media) is stuck in CreateContainerConfigError and its partner-api Secret doesn't exist — while the billing side is perfectly healthy. Nothing crashed. Read the SecretSync's status, not the Pod's logs, to find why the Secret was never materialized, and fix it.",
  "details": {
    "intro": {
      "text": "intro.md",
      "background": "background.sh",
      "foreground": "foreground.sh"
    },
    "steps": [
      { "title": "Diagnose the missing Secret", "text": "step1/text.md" },
      { "title": "Fix it and verify", "text": "step2/text.md", "verify": "step2/verify.sh" }
    ],
    "finish": { "text": "finish.md" }
  },
  "backend": {
    "imageid": "kubernetes-kubeadm-2nodes"
  }
}
